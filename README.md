# Installing development dependencies

```shell script
npm install
```

# Building
Start `sbt shell`:

```shell script
sbt
```

Inside `sbt shell`:
```sbtshell
compile
fastOptJS
```

Open `index.html` in a browser.
